# A～F 集成端到端验收证据

更新时间：2026-09-22

## 判定

- 软件工程 G1～G8：PARTIAL（部分通过）。G1～G4、G6～G8 已通过；G5 的真实命令、断线自动重连、令牌重放/过期/跨实例拒绝已通过，但严格 Linux POSIX/PTTY（伪终端）的尺寸变化证据未完成。
- 采购内容资源：PASS_AUTOMATED（自动化通过）。正式题库、12 套正式实验文件包、37 份理论 PPT（演示文稿）和 49 个讲解视频已交付并通过系统独立账号审核；固定开发库联合动态审计为 196/196、阻断 0。
- 生产验收：NOT RUN（未运行）。本记录只证明本地开发环境。

## 当前环境

- 分支：`integration`
- 执行时源码：`integration` 分支，包含真实流量采集、控制面制品登记、授权 ZIP（压缩包）下载和终端门禁加固；最终本地提交标识见本轮交付结果
- MySQL（数据库）：8.4.11
- Docker（容器环境）：29.7.2，Linux/amd64
- 固定运行镜像：`python:3.11-bookworm@sha256:35d3a4a3d5e42e02ab916d44513a050689f12c0533d45598d229672503fe77ca`
- 总控执行入口：`scripts/run-integration-gates.ps1`
- Alembic（数据库迁移）最新版本：`20260922_0017`
- 本次运行标识：`20260922c102`

总控脚本为后端、真实运行、43 人课堂和成绩归档分别创建独立门禁库；每个库均从空库执行全部 Alembic（数据库迁移）后再运行验收，不复用生产数据。

## 自动化结果

| 范围 | 结果 | 真实证据 |
|---|---|---|
| P0（优先级 0）契约 | PASS（通过） | OpenAPI（接口契约）重新生成后无差异 |
| 后端 | PASS（通过） | 201 项 pytest（后端测试）通过，使用四个隔离的真实 MySQL（数据库）门禁库及最新迁移 |
| 前端 | PASS（通过） | 14 个测试文件共 25 项单元测试通过，接口契约无差异，生产构建通过 |
| G1/G2 | PASS（通过） | 浏览器创建课程/班级、导入 43 人、逐行错误文件、学生分页搜索、真实签到、投票、作业和测验 |
| B 资源流水线 | PASS_AUTOMATED（自动化通过） | 浏览器读取 37+12 权威课时目录、上传真实文件、下载 XLSX（电子表格）模板、导入 196 行题库、逐行错误反馈、创建者隔离和独立审核发布；真实 MySQL（数据库）专项测试另验证 196/196 正式题目、12/12 实验文件包、37/37 理论课件和 49/49 真实讲解视频 |
| G3 | PASS（通过） | 规范 RSA（非对称加密算法）模板、不可变版本、预检、冻结和 JSON（数据文件）导出 |
| G4/G6 | PASS（通过） | Linux Docker（容器环境）真实双容器、网络隔离、5 个检查点 100 分、重建保分、幂等启动与销毁；重建/销毁前停止采集，3 份真实 PCAP（抓包文件，5761/606/606 字节）经登记、下载、解压、大小和 SHA256（文件校验值）复核通过 |
| G5 浏览器 | PARTIAL（部分通过） | Chromium（浏览器）启动容器并经 WebSocket（网页双向连接）终端执行真实命令；主动断开后领取新令牌自动重连并再次执行命令；首帧令牌、重放拒绝、30 秒真实过期拒绝和跨实例拒绝均通过。Windows（操作系统）节点代理不提供 POSIX/PTTY（伪终端），因此尺寸变化明确跳过；外部 Linux Node Agent（节点代理）未运行 |
| D 流量采集与 D→E 授权日志下载 | PASS（通过） | 控制面只有在真实抓包启动后才报告运行，销毁/重建/恢复均先停止、取回并校验 PCAP，采集失败仍清理运行组；普通学生跨用户下载被拒绝，篡改、过期、跨班、跨发布和重放审计由真实 MySQL（数据库）测试覆盖；本轮真实门禁进一步消费短期下载能力并复核 ZIP（压缩包）内 PCAP 摘要 |
| D 容量阶梯 | PASS（通过） | 在本次 `20260922c102` 运行库继续执行 5→10→20 请求测量，真实运行始终为 3 组，排队为 2/7/17，失败为 0；每组均启用真实抓包旁车，最终清理通过 |
| G7 | PASS（通过） | 43 人真实名单与运行事实聚合，30 人已启动、18 人运行、10 人提交、2 人失败、13 人未开始；实时更新和教师提醒通过 |
| G8 | PASS（通过） | 14 条上游事件投递，94/68 分、学情三个维度、归档六项检查、不可变产物下载和学生本人总评通过 |

## 明确不包含的验收

1. 浏览器主链使用的上传文件和课程数据是门禁库测试事实；正式题库、实验文件包、理论课件和视频以版本化内容源、实物文件、隔离测试与独立系统账号审核为证据，但尚未进行外部教研专家人工验收。
2. 37 份理论课件共 300 页已逐页渲染；49 个视频已核对媒体流、时长、哈希、字幕和抽帧。这些证据证明文件可解析、系统链路可用，不等同于教学专家内容验收。
3. 持久内容库联合动态审计已经达到 196/196、阻断 0；该结果只能作为本地自动化工程验收，不能扩展为生产验收结论。
4. 本轮容量测量显示单节点真实容量为 3 组；20 是请求总数而非同时运行数，不能宣传为 20 或 50 组同时运行。
5. 未执行生产服务器、正式域名、正式账号或真实生产数据验收。
6. 当前真实下载验证使用受控本地存储提供方；独立对象存储提供方、远程 Linux Node Agent（节点代理）跨主机传输和生产保留策略尚未验收。
